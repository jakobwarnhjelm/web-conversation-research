{
  "name": "@tabflow/domain",
  "version": "0.1.0",
  "description": "TabFlow spårsagnostisk domänkärna (M0): datamodell, block-kommandon, serialisering/migrering.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
